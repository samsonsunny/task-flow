# Archived PRDs (historical record only)

These six MVVM-refactor PRDs are **superseded** — all 24 stories completed in June–July 2026, and the code has evolved past them (file paths in the JSONs reference the pre-restructure `Features/Reminders/` layout, which no longer exists).

The knowledge they contained was consolidated into feature-oriented concept docs:

| Archived PRD | Consolidated into |
|---|---|
| `mvvm-reminder-segment.json` | `docs/concepts/task-segments.md` |
| `mvvm-completed-view.json` | `docs/concepts/task-segments.md` |
| `mvvm-lists-tab.json` | `docs/concepts/lists-and-groups.md` |
| `mvvm-list-detail.json` | `docs/concepts/lists-and-groups.md` |
| `mvvm-reminder-editor.json` | `docs/concepts/editor-and-scheduling.md` |
| `mvvm-schedule-picker.json` | `docs/concepts/editor-and-scheduling.md` |

Do not run the Ralph loop against these files — they contain no open stories. New PRDs go in `.agents/tasks/`.
