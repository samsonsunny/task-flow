### US-001: Create ListDetailViewModel with core properties and update() entry point
Status: in_progress
Depends on: None

Description:
Create the ViewModel file with @Observable class, init, update() entry point, and computed/derived state properties.

Acceptance Criteria:
- [ ] File TaskFlow/Features/Reminders/ViewModels/ListDetailViewModel.swift exists with @Observable class
- [ ] init(modelContext:listID:) stores both parameters
- [ ] update(tasks:lists:allTasks:now:) stores data and recomputes derived state
- [ ] list, tasks, rootTasks computed properties added
- [ ] flatNodes, flattenTasks(), flattenNode() with collapse support added (task 1.5)
- [ ] collapsedTasks set with toggleCollapse(_:) added (task 1.6)
- [ ] justCompleted set with toggleCompletion(for:) including animation delay added (task 1.7)
