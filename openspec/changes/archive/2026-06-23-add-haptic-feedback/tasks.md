## 1. Add haptic to ReminderSegmentDetailView

- [x] 1.1 In `toggleCompletion`, add `UIImpactFeedbackGenerator(style: .medium).impactOccurred()` before the `withAnimation` block

## 2. Add haptic to ListDetailView

- [x] 2.1 In `toggleCompletion`, add `UIImpactFeedbackGenerator(style: .medium).impactOccurred()` before the `withAnimation` block
