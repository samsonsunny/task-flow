## No Changes

The existing `task-row-display` spec already defines all requirements for this change:
- Chevron control at trailing edge (Requirement: Task row shows collapse/expand control for parents)
- Depth-based indentation (Requirement: Task row adapts indentation based on depth)
- Expanded hit targets (Requirement: Task row buttons have expanded hit targets)
- Subtask count in metadata (Requirement: Subtask count appears in metadata)
- Segment view suppression (Requirement: Segment views suppress nesting indicators)

No spec modifications needed — this change is purely implementation.
