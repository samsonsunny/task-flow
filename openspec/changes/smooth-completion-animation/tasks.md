## 1. Fix Timeline Position Jump

- [x] 1.1 Change `TimelineViewModel.update()` to sort `justCompleted` tasks naturally instead of appending

## 2. Add Strikethrough

- [x] 2.1 Add `.strikethrough(true, color: AppTheme.colors.textSecondary)` to the title `Text` in `TaskRowView`

## 3. Sync Spec

- [x] 3.1 Sync modified `completion-animation` spec to main specs

## 4. Verify

- [x] 4.1 Build with `xcodebuild`
